# Action

::: dotflow.core.action.Action
    options:
        members:
            - func
            - task
            - retry
            - params
            - _run_action
            - _set_params
            - _get_context
            - _get_task
