# Action

::: dotflow.core.action.Action
    options:
        members:
            - func
            - task
            - retry
            - timeout
            - retry_delay
            - backoff
            - params
            - _run_action
            - _set_params
            - _get_context
            - _get_task
