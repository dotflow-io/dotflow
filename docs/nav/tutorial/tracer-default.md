# Tracer Default

`TracerDefault` is the default tracer provider. It does nothing — no traces or spans are created.

This is the provider used when no tracer is explicitly configured in `Config`.

## References

- [TracerDefault](https://dotflow-io.github.io/dotflow/nav/reference/tracer-default/)
- [Tracer OpenTelemetry](https://dotflow-io.github.io/dotflow/nav/tutorial/tracer-opentelemetry/)
